interface:
  display_name: "Web"
  short_description: "Repo-specific patterns and workflows for web"
  default_prompt: "Use the web repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true